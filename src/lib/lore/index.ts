/**
 * Lore package entry: domain model, entity scan/persistence, and gallery
 * helpers. AI-assisted generation lives in ./generator (import it directly —
 * it pulls in the AI client).
 */

export * from "./model";
export * from "./entity";
export * from "./gallery";
