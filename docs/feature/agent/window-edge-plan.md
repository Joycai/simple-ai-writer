# 窗口边缘：卡死、重读循环与「完成了却什么都没写」

> 状态：`implemented`（2026-09-11 实测；方案经作者批准，按 PR-1 → PR-2 → PR-6 → PR-3 → PR-4(a) → PR-5 执行，六片全部实施；PR-5 待合并与真机）
> 起因：作者报告本地小模型（qwen3.8-27b，LM Studio，32k）「经常卡死、死循环、突然中断」。拿真实运行时对着真实端点量了一遍，三个症状对应到六个机制——其中五个**跟窗口走，不跟模型走**：同样的上限压到 DeepSeek 上，它一样重读、一样把轮次花在记账上，只是每轮快十倍，所以看不出来。
> 相关：[`edit-loop-plan.md`](edit-loop-plan.md)（「省一轮 ≈ 一整份工具表」的量纲）· [`agent-tool-context.md`](agent-tool-context.md)（常驻工具的成本账）· [`compact-threshold-plan.md`](compact-threshold-plan.md)（归纳触发线）· [`../../api/streaming.md`](../../api/streaming.md)（失败怎么送达）
> 台架：`scripts/local-model-probe.ts`（§7）

## 1. 结论

**小模型不是另一类问题，而是开局就站在窗口边上。** 助手档的常驻工具 schema ≈ 10.8k token、系统层 ≈ 3.4k，32k 窗口在默认「窗口占用」50% 下的输入上限是 16k——固定成本占上限的 89%，留给整段对话的是 **≈1.8k**。200k 的模型同样的代码路径要到长对话的后半段才碰到。

对照实验把这件事钉死了（§2.3）：

- **同一个 qwen、同一个任务，只把占用从 50% 调到 90%**：7 轮 223 秒、一个字没改 → 4 轮 49 秒、改成。
- **同一个 50%/32k 上限，换成 DeepSeek**：它也重读了四次、13 轮才改成，还在任务日志里写下「回执被上下文裁剪丢弃，未能读到批准结果」。

所以修法是**跟窗口挂钩的不变量**，不是「如果是本地模型」的分支——这和整个项目「不按 id 分支」是同一条规矩。

只有一条真正属于小模型：**思考打转**（§3 M6）。但它的分量一点也不轻——在写作类任务上它是 qwen「卡死 + 突然中断」的主因，而且**窗口宽裕时照样发生**：续写档上限 14k、估算不到 3k，它照样思考了 307 秒、把 32k 写满、一个字的正文都没有。更糟的是运行把这种结局报成 `completed`（M3），所以作者看到的只是「转了五分钟，然后什么都没有」。

## 2. 实测（2026-09-11）

### 2.1 台架与端点

| | qwen3.8-27b-uncensored | deepseek-flash |
|---|---|---|
| 端点 | LM Studio `192.168.2.206:11234`，OpenAI 兼容，无 key | 官方 `api.deepseek.com` |
| 量化 / 窗口 | IQ3_XXS，**加载 32,000**（模型上限 262,144） | 窗口人为设为 32,000（`contextSize`），用来隔离「窗口」这一个变量 |
| 速度 | 生成 ≈ 50 tok/s；冷预填充 13k token 8.7 秒，热（前缀缓存命中）0.6 秒 | 每轮 1–4 秒 |
| 思考 | 默认开，`reasoning_content`，思考用英文 | 默认开 |

两边跑的都是 `scripts/local-model-probe.ts`：真实 `runAgent`、真实工具 schema、真实工具处理函数；Tauri 文件系统换成内存项目，审批一律自动通过并落盘。

### 2.2 上限算出来是多少（`messageCeilingForTools`，32k）

| 档 | 常驻工具 | 占用 50% | 70% | 90% |
|---|---|---|---|---|
| 助手 `full` | 10,828 | **5,172** | 11,572 | 17,972 |
| `write` | 4,707 | 11,293 | 17,693 | 24,093 |
| 续写 `read` | 1,972 | 14,028 | 20,428 | 26,828 |

这个「消息上限」**包含系统层**（`trimHistory` 数的是整段 history，`history[0]` 就是系统提示）。助手的系统层实测 4,577 字、估算 3,364 token，所以 50% 下留给对话的是 1.8k。

### 2.3 运行

| 场景 | 模型 | 占用 | 轮 | 用时 | 裁剪 | 结果 |
|---|---|---|---|---|---|---|
| `chat-edit` 改一段（16 行文件） | qwen | 50% | 5 | 65s | 1 | 改成。1 轮被检查点提示引去写笔记（台架早期版本没给工作区，报错） |
| `chat-longdoc` 改一段（52 行） | qwen | 50% | 6 | 56s | 2 | 改成。**2/6 轮是记账笔记**，第 5 轮那条标题是「已完成」 |
| `chat-bigdoc` 改一段（105 行，段落重复两次） | qwen | 50% | 7 | 223s | 3 | **没改成**。结果刚到就被裁 → 重读同一页 → 第 7 轮思考 171 秒 / 59,777 字后撞满 32k 截断 → 报 `completed`，输出 **0 字** |
| `chat-bigdoc` | qwen | **90%** | 4 | 49s | 0 | 改成 |
| `chat-edit` | deepseek | 50% | 4 | 10s | 1 | 改成。记账笔记与改写同轮发出 |
| `chat-longdoc` | deepseek | 50% | 8 | 28s | 4 | 改成。**5/8 轮是笔记 / 清单**，一次裁掉 9 条 |
| `chat-bigdoc` | deepseek | 50% | 13 | 55s | 9 | 改成（occurrence=2）。整篇读、从第 1 行读、从第 50 行读、从第 54 行读——每次都被裁；**它自己那次写入的批准回执也被裁掉了** |
| `chat-survey` 通读三章答问 | deepseek | 50% | 7 | 27s | 4 | 答出（1,262 字）。读完三章的下一轮先被检查点提示引去写笔记，再下一轮三章原文连同其余结果一次被裁掉 7 条；之后 3 轮是笔记 / 清单，估算因笔记参数涨到上限之上 |
| `continue` 续写 800 字 | deepseek | 50% | 3 | 14s | 0 | 写出（925 字）。续写档常驻工具只有 2k，上限 14k，没碰到边 |
| `longform` 写 3000 字短篇（无工具） | qwen | — | 1 | 358s | — | 输入 326，输出 **31,671 token 全是思考**（42k 字，在几个情节方案之间来回），正文 0 字，撞 32k 截断，报 `completed` |
| `longform` | deepseek | — | 1 | 65s | — | 写出（2,412 字），输出 10,969 token，未截断——**同一个提示，思考打转只发生在 qwen 身上**（M6 的对照） |
| `continue` 续写 800 字 | qwen | 50% | 4 | 315s | 0 | **没写出**。上限 14k、估算最高 2.9k，窗口毫无压力；读了三份文件后第 4 轮思考 **307 秒 / 117,766 字**，把 32k 窗口整个写满后截断，正文 0 字，报 `completed`。同一任务 DeepSeek 14 秒 |
| `chat-survey` 通读三章答问 | qwen | 50% | 4 | 51s | 1 | 答出（1,025 字）。三章短，读完之后才裁到一条 |
| `continue` · **思考关**（`reasoning_effort: "none"`） | qwen | 50% | 8 | **20s** | 0 | 写出（819 字），未截断。七个工具轮各只调一个工具，第 8 轮（强制成文）写出正文 |
| `longform` · **思考关** | qwen | — | 1 | **32s** | — | 写出（2,716 字），输出 1,669 token，未截断 |
| `chat-bigdoc` · **PR-1 之后** | qwen | 50% | 7 | **55s** | 2 | 改成。读完 105 行之后估算停在 5,767（上限 5,172 之上）——那次读取的结果留下了，只裁掉更早的两条；之前同一设置是 223 秒零产出 |
| `chat-bigdoc` · **PR-1 之后** | deepseek | 50% | **4** | **14s** | 2 | 改成。之前是 13 轮 / 55 秒、四次重读 |
| `continue` · **PR-2 之后**（思考开） | qwen | 50% | 4 | 330s | 0 | 仍然零产出（第 4 轮思考 112,042 字写满窗口——那是 PR-6 的事），但结局从 `completed` 变成 **`truncated`**，事件带 `cause=window` + `thinkingOnly`，日志说「思考写满了上下文窗口，没有写出回答」并指向「思考」档位 |
| `longform` · **PR-6 兜底**（思考开） | qwen | — | 2 | **222s** | — | **写出（3,138 字）**。第 1 轮思考 15,936 字、186 秒时被中止，关思考的重答 36 秒写完。之前 358 秒、0 字 |
| `continue` · PR-6 **初版**（只有重答那一次关思考） | qwen | 50% | 6 | 543s | 0 | **比没有兜底还久**。第 4 轮 155 秒被中止；关思考的重答恰好是个工具轮（又读一章）；第 6 轮思考照开，又打转 383 秒、75,235 字写满窗口，结局 `truncated`。据此改成「这次运行余下的请求都关思考」（D6 落地说明） |
| `continue` · **PR-6 定稿**（运行余下的请求都关思考） | qwen | 50% | 9 | **143s** | 0 | **写出（1,068 字）**。第 4 轮思考 31,908 字、124 秒时被中止；之后每轮关思考、约 1 秒一轮，第 9 轮（强制成文，重来那一轮不占作者的轮数）写出正文。副作用：不思考的 qwen 把第三章同一页连读了三次（第 6–8 轮，各约 1 秒）——重复读检测按 D2 的取舍没有做 |
| 挂起的端点 · **PR-3 之后** | 本地假服务（回 200 + SSE 头之后一个字节都不发） | — | 1 | **123s** | — | 以前会永远等下去；现在 123 秒报 `StreamStallError`：「模型 123 秒没有返回任何内容，已停止等待。本地模型请确认服务还在运行、模型已经加载……」，假服务那一侧同时看到连接被关闭 |
| `chat-longdoc` · **PR-4 之后** | deepseek | 50%（按保底实际约 69%） | **4** | **13s** | **0** | 改成。消息上限 5,172 → 11,200：读两页、改、答，**没有一轮花在笔记或清单上**。之前同一设置 8 轮 / 28 秒、裁 4 次、5/8 轮记账 |
| `chat-longdoc` · **PR-4 之后** | qwen | 50%（按保底实际约 69%） | 5 | 55s | **0** | 改成。**没有一轮花在笔记上**，也没裁过。用时和之前（6 轮 / 56 秒、裁 2 次、2/6 轮记账）差不多——大头是第 4 轮 32 秒的思考，省下的是那两轮记账 |
| `chat-bigdoc` · **PR-4 之后** | qwen | 50%（按保底实际约 69%） | 5 | **48s** | **0** | 改成。105 行整篇读进来一次也没被裁，**在默认设置下追平了手动调到 90% 的那组对照**（4 轮 / 49 秒）。这个场景的全过程：PR-1 之前 223 秒零产出 → PR-1 之后 55 秒 → PR-4 之后 48 秒且零裁剪、零记账 |
| `chat-session` 六轮对话 · 默认线（PR-4 之后） | qwen · 思考关 | 50% | — | 33s | — | 触发线 7,839，**一次都没归纳**（历史 3,368 → 7,214） |
| `chat-session` 六轮对话 · 默认线（PR-4 之后） | deepseek | 50% | — | 44s | — | 第 5 轮归纳一次 8,102 → 6,374（落回线下，4.2 秒），之后不再归纳 |
| `chat-session` · 线压到 4,000 · **PR-5 之前** | qwen · 思考关 | 50% | — | 80s | — | **第 4、5、6 轮每次都归纳**，回收 976 / 549 / 396 token，归纳请求 9.2 / 15.4 / 19.2 秒——归纳请求没带上「思考关」 |
| `chat-session` · 线压到 4,000 · **PR-5 之后** | qwen · 思考关 | 50% | — | 59s | — | **一次都没归纳**：折不回线下、历史（最高 8,677）又没到上限的 90%（10,080），全部延后 |
| `chat-session` · 线压到 4,000 · 强制归纳 · **PR-5 之后** | qwen · 思考关 | 50% | — | 68s | — | 三次归纳各 **3.9 / 3.9 / 4.0 秒**，不再逐次变长——归纳请求带上了「思考关」 |

两条直接用 curl 打的端点探针：

- **撞窗**：22,453 token 的提示 + 不设上限的输出 → 首字 14.4 秒，思考 8,263 + 正文 1,284 = 恰好 32,000，`finish_reason: length`，断在半个词上，218 秒。
- **超窗**：66k token 的提示 → 0.8 秒返回 HTTP 200 + SSE `event: error`（`exceed_context_size_error`，带 `n_prompt_tokens` / `n_ctx`）。适配器的 `json.error` 分支接得住，这条路没问题。

### 2.4 流的形状

- **工具参数一次到齐。** LM Studio 把 `tool_calls[].function.arguments` 攒到生成完才发一整块——模型写一条长参数的这段时间流是静默的（实测最长 7.4 秒；一整章的 `rewrite_document` 按 50 tok/s 算是分钟级）。`toolArgsProgress` 对它无能为力。
- **前缀缓存是本地的命脉。** 同一轮次序列里冷预填充 8.7 秒、之后每轮 0.6–0.8 秒；而 `trimHistory` 改的是 history **中间**的消息，改完那一轮的首字回到 2.7 秒。
- **估算器对中文偏保守。** `estimateTextTokens` 把一个汉字算一个 token，Qwen 的分词实测约 0.6 token/字。方向是安全的（早裁不晚裁），记下不改。

## 3. 机制

### M1 裁剪会删掉模型这一轮刚拿到的东西（重读循环）

`runtime.ts` 的 `trimHistory` 从最早的 `role: "tool"` 往后逐条替换成占位符，直到估算回到上限以下。它**不认识轮次**：只要不可裁的部分——系统层、用户消息、注入、助手正文、**以及所有工具调用的参数**——已经逼近上限，最新的那条结果就会在模型读到它之前被替换掉。

模型看到的是 `[earlier tool result dropped …]`，唯一合理的动作是再读一次；再读一次又被裁。强模型会换着页码读、会写笔记绕开（DeepSeek 13 轮），弱模型会读到轮数用完或者在思考里打转（qwen 223 秒、零产出）。

两个加重因素：

- **工具调用参数从来不裁。** `write_note` 把一整份分析写进参数、`propose_edit` 把整段原文放进 `find`——这些都留在 history 里，而且恰恰是本来就已经落盘的东西。DeepSeek 的 survey 跑到估算 7.4k（上限 5.2k）之后，每一轮都会把刚到的全部结果裁掉。
- **改 history 中间的消息会打掉前缀缓存**（§2.4），本地模型每裁一次就多付一次预填充。

### M2 检查点提示在小上限上变成每隔一轮一次

`CHECKPOINT_RATIO = 0.85`：估算超过上限的 85% 就注入「用 write_note 把结论写进笔记」。`checkpointArmed` 在**每次**裁剪后被重置——而在小上限上几乎每轮都裁、估算几乎永远在 85% 以上，于是提示每隔一轮就回来一次。再加上系统层「任务清单」一节要求逐步 `task_progress`，模型把轮次花在记账上：qwen longdoc 2/6，DeepSeek longdoc 5/8。

每一轮都要把 10.8k 的工具表重发一遍（`edit-loop-plan.md` §1 的量纲），在本地模型上每一轮还是几秒到几十秒。

### M3 思考吃掉窗口，截断被当成「完成」

三处缺口叠在一起：

1. **没有给输出留位置。** OpenAI 兼容适配器不发 `max_tokens`（`modelLimits.ts` 的「planning-only」约定），`streamCompletion` 的预检只比 `input ≤ contextSize`。于是一次 22k 输入的请求在 32k 窗口里只剩 9.5k 给思考加正文——而 qwen 光思考就要 8k。
2. **只思考、没正文的截断不算截断。** `runtime.ts` 的恢复条件是 `roundTruncated && roundText.trim()`；思考把窗口用完、正文一个字没出的那一轮，既不恢复也不报错，运行以 `outcome: "completed"` 和空输出结束。`longform` 和 `chat-bigdoc` 都是这样收场的。
3. **归因说错了。** 日志那一行是「输出被上限截断，回答未写完」——但既没有「输出上限」这回事（是窗口满了），也没有「未写完」的回答（一个字都没开始）。作者照这句话去调输出上限，调多少都没用。

另外，对「窗口满了」这种截断走「接着写」恢复是只会更糟的：续写请求带着更长的 history 回去，剩给输出的只会更少。

### M4 流没有看门狗

四个适配器的 `reader.read()` 都没有超时；`AgentLog` 的轮次秒表从轮次开始一直走，分不清「在预填充」「在静默地写工具参数」「连接已经死了」。本地服务被卸载模型、中转层断流而不关连接，界面就永远停在「思考中… 3:47」。

这一条和窗口无关，是任何端点都会有的——只是本地模型的正常静默更长（冷预填充十几秒、长工具参数几十秒），所以更容易被读成卡死，看门狗的阈值也因此不能拍脑袋定得很紧。

### M5 小窗口上几乎每次发送都先归纳一次（读代码得出，未实测）

`compactTriggerFor` 的第三条线是 `0.7 × 消息上限` = 3,620（32k、50%），这个数**包含系统层**。`planFold` 折叠后保留 `prelude + SUMMARY_BUDGET_TOKENS(1000) + 最近 2 轮`，光前两项就 ≈ 4.4k，已经高于触发线——而 `planFold` 不检查「折完之后是否回到触发线以下」。于是对话一旦超过两轮，**每次发送前都先让同一个本地模型做一次总结**，折完仍在线上，下一次再折。

对作者来说是「点了发送，很久没有反应」。台架不跑对话归纳，这一条要在真应用里确认（§5 PR-5）。

> **2026-09-11 实测（PR-5 动手前，台架加了按 `sendChat` 顺序逐轮归纳的多轮对话 `chat-session`）**
> - **默认 32k 上 PR-4 之后已经不复现。** 消息上限 11,200、触发线 7,839：qwen 六轮一次都没归纳（历史 3,368 → 7,214），DeepSeek 在第 5 轮归纳一次（8,102 → 6,374，落回线下），之后不再归纳。
> - **机制本身是真的**，只是需要触发线低于「折完剩下的固定部分」（系统层 + 1,000 的摘要预算 + 保留原文的两轮）：把线压到 4,000，六轮里第 4、5、6 轮**每次发送都归纳**，回收 976 → 549 → 396 token，归纳请求却要 9.2 → 15.4 → 19.2 秒，是它前面那一轮本身的两到三倍。更小的窗口、更重的种子块、或者大模型上把 token 滑块拉低，都能走到这里。
> - **顺带查到一个更直接的原因**：`summarizeForCompaction` 手抄了一份连接字段，漏掉思考档位、思考预算和温度——作者把思考调成「关闭」，归纳请求照样思考。全应用只有这一处手抄（其余调用点都展开 `connOptions` / `pickConnOptions`）。

### M6 思考打转（模型侧）

qwen 在 `longform` 里写了 42k 字的思考、在 `chat-bigdoc` 第 7 轮写了 60k 字、在 `continue` 第 4 轮写了 **117,766 字（307 秒）**，内容是在几个方案之间来回（「或者更戏剧……太狗血」「可以写现实悬疑……」），**不是逐字复读**——n-gram 式的退化检测抓不到它（Sakura 的 tok/行 判据也不适用）。能管住它的只有长度或时间预算，或者干脆关掉思考。

它和窗口无关：`continue` 那一轮的输入只有 4.8k，是思考自己把剩下的 27k 写满的。同一批提示上 DeepSeek 的思考都是有界的（`longform` 65 秒写出 2,412 字、`continue` 14 秒）。这是六条里唯一真正属于小模型（加上 IQ3 低量化）的一条，**也是写作类任务上代价最大的一条**。

## 4. 设计取舍

**D1 修不变量，不做模型分支。** §2.3 的两组对照说明问题跟上限走；按「本地 / 小模型」开关修，大窗口模型在长对话尾部照样会踩，而且判断「谁是小模型」本身就是一个会过时的名单。

**D2 本轮拿到的结果永远不裁。** `trimHistory` 的可裁范围截止到**最后一条带 `tool_calls` 的助手消息之前**；那之后的工具结果、图片跟帖，以及当前这一问，一律不动。其次，把**早先轮次**里写工具的大参数（`write_note.content`、`create_file.content`、`propose_edit.find/replace`……——都已落盘或已应用）替换成合法 JSON 的占位对象，这是 M1 里真正长大的那部分。两步之后仍然超，就让这一轮照发：真超窗时预检闸门会报 `ContextSizeError`，比静默删掉模型刚读的东西诚实。

> 不选「检测重复读、直接告诉模型你读过了」：那是在治症状——模型再读一次，恰恰是因为它真的需要那段内容。

**D3 消息上限要有保底。** 「窗口占用」的本意是「单次请求占窗口多少」，但当固定成本吃掉上限的 89%，这个设置的实际效果是「不能工作」。三个选项：

- (a) 上限 = `max(窗口 × 占用, 固定成本 + 工作区保底)`，再封顶在 `窗口 − 输出预留`；上下文条上说明「为了留出工作区，本次按 N% 计」。
- (b) 只警告，不改数。
- (c) 固定成本过高时自动降档（`full` → 工具包模式 / `write`）。

推荐 (a)：它是 §2.3 那组 90% 对照的自动版本，而且只在上限小到不能用时才生效。**这一条改了一个设置的语义，需要作者定。**

> **PR-4 落地（作者选定 a）**
> - **一个函数**：`lib/context/budget.ts` 的 `effectiveInputCeiling(窗口, 占用, 工具 token)` = `max(窗口 × 占用, min(工具 + ⌈窗口 × 35%⌉, ⌊窗口 × 75%⌋))`。只抬不降；没声明窗口的照旧用假定上限。
> - **所有生产者都走它**：对话与扮演的运行（`messageCeilingFor`）、包子运行与一致性核对（`messageCeilingForTools` / `planReview`）、AI 面板的规划（`planContextBudget`，之后照旧再扣输出预留），以及对话、扮演两条计量条的分母——条上画的上限和循环裁到的上限必须是同一个数（context-meters.md 的「段的合计 = 上限」）。
> - **动了谁**：32k、默认 50%、助手档，输入上限 16,000 → **22,028**（约 69%），消息上限 5,172 → **11,200**。32k 上 `write` 档（工具 4.7k）、续写档、扮演都**不变**——它们的工具没有挤到 35% 以下；没有工具的运行永远不变（占用下限 50% 已经高于 35%）。16k 窗口的助手档封顶在 75%（12,000），消息仍只有 1.2k——那是工具档本身的问题，保底救不了。
> - **说出来**：计量条展开图例时多一句「「窗口占用」设的是 50%，但工具说明占掉了其中大半——为了给对话留出工作空间，本次按 69% 计。」判断来自 `ContextBreakdown.raisedFromTokens`（调用方把作者自己那份上限一并传进来）。
> - **没做**：AI 面板的分配条和设置页「窗口占用」一行不写这句——分配条的上限随之变化但不解释，设置页的例子句照旧用 `messageCeilingFor` 算出触发线，数是对的。

**D4 检查点提示限频，而不是只发一次。** 最初写的是「每次运行至多一次」，动手时查到 [`subagent-lld.md`](subagent-lld.md) 那条**有意为之**的记录：第一版正是只发一次，结果「长任务照样丢」，所以才改成每裁一次就重新布防。两件事都对——大窗口的长任务需要被再提醒，小窗口不能隔轮提醒一次。落地的规则是：**裁剪真的丢过东西才重新布防，且两次提示至少隔 `CHECKPOINT_MIN_GAP_ROUNDS`（4）轮**。对照实测：qwen longdoc 两次提示隔 2 轮，新规则下只剩第一次；30 轮的长任务照样每 4 轮以上被提醒一次。

**D5 截断要分清是谁满了。**

- 预检加输出预留：`input + reserve ≤ contextSize`，`reserve` 取 `effectiveMaxOutput` 与一个思考余量中较小者（口径与 `budget.outputReserveTokens` 对齐，不另起一套）。
- 新增截断归因：`window-full`（`input + output ≥ contextSize − 余量`）与 `output-cap` 分开；**只有思考、没有正文**的截断单列一类，运行结局不再是 `completed`，日志说「思考用完了窗口，没有写出回答」。
- `window-full` 不走自动「接着写」，直接出截断卡——续写只会让它更满。
- 不改线上字段：OpenAI 兼容这边照旧不发 `max_tokens`（「未设置的模型请求体不变」这条不变量不动）。

> **落地时的两处调整（PR-2）**
> - **预检的输出预留挪到 PR-4。** 估算器把一个汉字算一个 token，而 Qwen 实测约 0.6——在发送闸门上硬扣一份预留，会把其实装得下的中文请求挡在门外，而且报的是 `ContextSizeError`，比截断更难懂。PR-4 的上限保底本来就封顶在 `窗口 − 输出预留`，那是规划侧的软版本，同一件事只做一处。
> - **窗口满了不出截断卡，直接以 `truncated` 结束。** 卡上的「继续」在窗口满时只会再截一次，把一个注定失败的按钮递给作者不是询问；部分正文照样留在输出里，日志那一行说清是窗口满了、该调什么。作者下一轮自己说「继续」时，对话归纳有机会先腾出位置。
> - 归因需要窗口大小和端点的 usage：`input + output ≥ 窗口 − max(64, 3%)` 记为 `window`，装得下记为 `output-cap`，**两者缺一就不下结论**、按原来的路恢复——没声明窗口的模型行为与今天完全一致。3% 的余量盖住「作者声明 32,768 / 服务端实际加载 32,000」这种常见落差。

**D6 思考：开关已经存在，缺的是让作者找得到、以及开着时的兜底。** 同一句短提示（上限 600 token）在 LM Studio 的 qwen 上逐个试开关：

| 请求体 | 思考字数 | 正文 | 结论 |
|---|---|---|---|
| （什么都不发） | 199 | 71 | 默认开 |
| `reasoning_effort: "none"` | **0** | 88 | **唯一管用的** |
| `reasoning_effort: "low"` | 193 | 64 | 被忽略 |
| `enable_thinking: false` | 194 | 66 | 被忽略 |
| `chat_template_kwargs.enable_thinking: false` | 201 | 70 | 被忽略 |
| `thinking: {type: "disabled"}` | 201 | 70 | 被忽略 |
| 提示词末尾 `/no_think` | 107 | 46 | 减半，不可靠 |

`reasoning_effort: "none"` 正是应用里「通用」思考类别（`openai-generic`，OpenAI 兼容端点的默认类别）档位选「关闭」时 `reasoningBody` 发出去的东西（`OPENAI_EFFORT.off = "none"`）。所以**作者今天就能把它关掉**，而且开关并不深：`ReasoningControls` 就在对话输入框底栏（`AgentChat`）、AiPanel 设置行和一致性检查里，档位菜单里有「关闭」（它改的是模型本身的设置，不是单次覆盖）。缺的是**动机**：档位显示「默认」、什么都不发，作者没有任何理由怀疑五分钟的空转是它造成的。

还有一个字面陷阱：**两个控件都叫「关闭」**。模型设置里的思考**类别**有一项「关闭」，说明是「不发送任何思考参数，由服务端自行决定」——在 LM Studio 上它的效果是**思考照开**；真正管用的是「通用 (reasoning_effort)」类别下、输入框底栏那个**档位**里的「关闭」。一个想关思考的作者在模型设置里点到的那个「关闭」，是不管用的那个。关掉之后，经应用自己的 `reasoningBody` 重跑那两个零产出的写作场景（台架 `LOCAL_LLM_EFFORT=off`）：`continue` 315 秒 → **20 秒**写出 819 字，`longform` 358 秒 → **32 秒**写出 2,716 字，都没有截断（§2.3 末两行）。代价是不思考的 qwen 更爱一轮只发一个工具调用——`continue` 用满了 8 轮，是最后那个强制成文的轮次写出的正文。

所以这一片分两半：①让关得掉这件事被看见（M3 的新截断归因里，「只有思考、没有正文」那一类直接指向思考设置）；②开着思考时的兜底——思考超过预算就中止本轮、带一句「直接作答」重试一次，预算默认取窗口剩余的一半。第二半等第一半落地、作者真机用过再定要不要做。

> **PR-6 落地（作者选定两半都做）**
> - **同名陷阱**：什么都不发的那个类别改名「不发送（服务端决定）」，提示补一句「这不会关掉思考……要关，选对应的类别，再把档位调成「关闭」」。档位里的「关闭」不动——它是真的关。
> - **兜底的触发**：本轮思考估算（`estimateTextTokens` 逐片累加）超过 `(窗口 − 本轮估算输入 − 工具 schema) × 0.5`（下限 512），且正文、工具参数、工具调用都还没开始。窗口大小未知就没有兜底（与 PR-2「缺一不下结论」同一条）。32k 窗口上约在一万出头 token 处触发；200k 窗口上线在十万 token 之外，正常运行碰不到。
> - **中止**：每轮一个自己的 `AbortController`，转发运行的 signal——截断的是这一个请求，作者按停止仍然是停整个运行。
> - **重答之后，这次运行余下的请求都不再思考**（每次运行至多中止一次）。模型自己的思考档位里有「关闭」（档位菜单含 `off`，或是开关型类别）时，余下每个请求都带 `reasoningEffort: "off"`——发出去的永远是作者自己也点得到的值；否则每个请求带一条「不要展开思考」的提示，发完即撤。**不写回模型设置**：下一次运行（对话的下一轮）照作者自己的选择来。
>   - 最初写的是「只有重答那**一次**请求关思考」，台架当场推翻了它：`continue` 被中止后的重答恰好是一个工具轮（又读了一章），下一轮思考照开，又打转 383 秒写满窗口，全程 543 秒、0 字——比不加兜底（330 秒）还久。所以关思考的范围是**这次运行**，不是一个请求。
> - **不花作者的轮数**：重答那一轮 `maxRounds + 1`，而且轮数上限卡不会因为它再问一次。
> - **日志**：仍是 `output-truncated`，`cause: "thinking-budget"`、`thinkingOnly`，`recovery` 新增 `thinking-off` / `answer-now` 两种；重答之后仍然写满窗口的，走 PR-2 的原路报出来。
> - 被中止的那次请求拿不到 usage，它花掉的思考 token 不进用量统计——本地模型不计费，托管模型上这是低估，记下不修。

**D7 看门狗放在 `streamCompletion` 一处。** 四个适配器共用：任何 chunk（正文、思考、`toolArgs`）都重置计时；「等首字」和「流中断」分两个阈值，首字的那个按估算输入放大（本地冷预填充量级 ≈ 1.5k tok/s）；超时抛一个带原因的错误，文案说清是哪一种。LM Studio 的工具参数静默是正常形态，所以间隔阈值要以分钟计，不以秒计。

> **PR-3 落地**
> - **量过再定阈值。** LM Studio 上让 qwen 用 `create_file` 写 2,826 字：工具名在 0.8 秒到达，之后**整整 32.6 秒一个字节都没有**，参数一次到齐——约每字 11.5 毫秒的静默。一章 15k 字的改写就是几分钟，一份 60k 字的 HTML 页面十几分钟。所以间隔阈值不能拍脑袋定紧。
> - **首字**：`120 秒 + 估算输入 ÷ 150 tok/s`（假设慢硬件的预填充；32k 满窗约 333 秒）。**首字之后**：任意两个 chunk 之间 10 分钟（按上面的速度约 5 万字的工具参数）。看门狗是给**死掉的流**的，不是给慢的流的。
> - 放在 `streamCompletion` 一处，任何 chunk（正文、思考、`toolArgs`、服务端工具）都算活着；自己的 `AbortController` 转发调用方的 signal，一个按需重排的计时器而不是每个 token 一次 `setTimeout`。
> - 超时抛 `StreamStallError`（`first-chunk` / `idle`），**不是** `AbortError`——运行时把 AbortError 读作「作者按了停止」，而一个端点不再供货的流是要告诉作者的失败。PR-6 的思考中止与作者的停止都照旧走 AbortError。
> - 阈值是常量，不进设置：作者没有理由调它，而调紧了会误杀本地模型的正常长静默。

**D8 归纳要有回差。** `planFold` 在「折完预计仍不低于触发线」时拒绝折叠，改为出一次说明（「窗口太小，归纳帮不上这一轮」）——和 `contextBreakdown` 里 `over` 与 `willCompact` 分家是同一个道理。

> **PR-5 落地**
> - **不是一律拒绝，是延后。** 按原文「拒绝」会让历史一路长到超窗——`planFold` 那条「折不到目标也尽力折」的保证正是为历史真的装不下时存在的。所以规则是：最大程度的折叠仍落在触发线之上、**且**历史还不到消息上限的 90%（`FOLD_DEFER_CEILING_SHARE`）时先不折；到了 90% 照折。作者手动的「立即归纳」（`force`）永不延后。对话和扮演的自动归纳都走这条；状态记忆本来就是 `force`，不受影响。
> - **计量条直接问 `planFold`**，不另抄一份规则：延后时 `foldDeferred` 为真，不画归纳线、不说「下一轮会归纳」，展开图例解释为什么先不折。
> - **顺带修归纳请求的连接字段**（M5 实测里查到的）：改用 `...pickConnOptions(config), serverTools: undefined`，与 `structured.ts` 同形。
> - 两条计量条测试原先用的玩具尺寸（每轮几十到几百 token）正好落进「折不回线下」，按「折一轮就能回到线下」的尺寸重写，另加两条专测延后。

## 5. 分阶段 PR

每片从 `main` 切，不叠分支；每片合并、真机试过再开下一片。

| PR | 内容 | 主要文件 | 怎么验 |
|---|---|---|---|
| **PR-1** ✅ | D2 本轮结果不裁 + 旧写参数瘦身（跳过自带重放副本的调用：Gemini parts / Responses items / Anthropic 签名思考）；D4 检查点限频 | `agent/runtime.ts`、`agentRuntime.test.ts`、`agentRuntimeCheckpoint.test.ts` | 单测：本轮不裁、答完一轮后不再保护、占位参数仍是合法 JSON 且短字段保留、自带副本的调用不动、提示不隔轮出现但长任务仍会再提醒；台架 `chat-bigdoc` 50%：qwen 223 秒零产出 → 55 秒改成，DeepSeek 13 轮 → 4 轮（§2.3） |
| **PR-2** ✅ | D5 截断归因 + 只思考的截断不再算完成（输出预留挪到 PR-4，见 D5「落地时的调整」） | `agent/runtime.ts`、`agent/events.ts`、`agent/logModel.ts`、`AgentLog.tsx`、两份 locale | 单测：窗口满不续写、输出上限照常续写、只思考标 `thinkingOnly`、窗口满时截断的工具调用不重发、窗口大小未知时行为不变、日志标题优先显示截断行；台架 `continue`（思考开）结局 `truncated` · `cause=window` · `thinkingOnly`（§2.3） |
| **PR-3** ✅ | D7 流看门狗：首字 `120 秒 + 估算输入 ÷ 150 tok/s`，首字之后两个 chunk 之间 10 分钟，超时抛 `StreamStallError` | `ai/index.ts`、`ai/types.ts`、两份 locale、`streamWatchdog.test.ts` | 假计时器单测：首字超时且不提前、开始输出后中断、慢而活着的流（首字 90 秒、之后每 5 分钟一块）不被误杀、作者停止仍是 AbortError、首字期限随输入放大；阈值依据是 LM Studio 实测工具参数前 32.6 秒零字节；本地挂起端点实测 123 秒报错并断开连接（§2.3） |
| **PR-4** ✅ | D3 消息上限保底，作者选 **a**：`effectiveInputCeiling` = `max(窗口×占用, min(工具 + 窗口×35%, 窗口×75%))`，所有生产者共用；计量条图例说明「本次按 N% 计」 | `context/budget.ts`、`agent/toolCost.ts`、`consistency/budget.ts`、`agent/contextBreakdown.ts`、`AgentChat.tsx`、`RoleplayChat.tsx`、`ContextBar.tsx`、两份 locale | 单测：抬高、只抬不降、无工具不变、封顶 75%、无窗口不抬、面板规划从它起算、32k 助手档消息上限恰为 35%、`raisedFromTokens` 只在抬高时有值；`contextForecast.test.ts` 两条原先钉着「被挤光」的用例按保底后的事实改写；台架 50% 默认：DeepSeek `chat-longdoc` 8 轮 → 4 轮、记账 5 → 0，qwen `chat-longdoc` 记账 2 → 0，qwen `chat-bigdoc` 48 秒零裁剪、追平 90% 对照（§2.3） |
| **PR-5** ✅ | D8 归纳回差（折不回线下且离上限还远时延后）+ 归纳请求带上模型的思考设置 | `agent/compact.ts`、`agent/compactRun.ts`、`agent/contextBreakdown.ts`、`ContextBar.tsx`、两份 locale | 单测：延后、接近上限照折、手动归纳不延后、计量条 `foldDeferred` 与关闭自动归纳时不延后；台架 `chat-session`（线压到 4,000）：归纳 3/6 → **0/6**；强制归纳时每次摘要 9–19 秒 → **约 4 秒**（§2.3）。作者真机：连续对话，执行日志不应每轮都有「已归纳」 |
| **PR-6** ✅ | D6 两半都做：「关闭」同名陷阱改名；思考超「窗口剩余一半」且未开始作答时中止本轮，这次运行余下的请求都不再思考（档位有「关闭」就发 off，否则每轮带一条即撤的提示） | `agent/runtime.ts`、`agent/events.ts`、`AgentLog.tsx`、两份 locale、`agentRuntimeThinkingGuard.test.ts` | 单测：中止并关思考重来且不占轮数、关思考延续到工具轮之后、无「关闭」档位时走提示且提示不留在历史里、每次运行至多一次、窗口未知不启用、预算内不中止、作者停止仍是停止；台架（思考开）`longform` 358 秒 0 字 → 222 秒 3,138 字，`continue` 330 秒 0 字 → 143 秒 1,068 字（§2.3） |

顺序的理由：

- **PR-1 先做**：助手档里代价最大的那几行（`chat-bigdoc` 零产出、DeepSeek 13 轮）都是它，而且它只收紧一个函数的行为、不碰任何设置。
- **PR-2 与 PR-6 紧随其后**：写作类任务上真正让作者白等五分钟的是思考打转（M6），PR-2 让它至少**被说出来**而不是报成完成，PR-6 让它**不再发生**。PR-6 的范围取决于 D6 那次端点探针——如果某个开关在 LM Studio 上管用，它就只是一份文档加一张内置表。

## 6. 风险

| 风险 | 缓解 |
|---|---|
| 不裁本轮结果后请求真的超窗 → 端点 400 | 预检闸门先报 `ContextSizeError`；D3 从源头给工作区留位置 |
| 改写旧工具参数后，Anthropic / Gemini 重放或 DeepSeek 的 `reasoning_content` 回传出问题 | 只换 `arguments` 字符串为合法 JSON 占位，不动消息结构和 `_` 字段；三族序列化各一条测试 |
| 看门狗误杀一次慢但正常的本地预填充 | 首字与间隔分开计；首字阈值随估算输入放大；阈值以分钟计 |
| D3 保底让实际占用超过作者设的百分比 | 上限永远 ≤ `窗口 − 输出预留`；条上明说按多少计；作者可以换小工具档 |
| 估算器对中文高估（1 字 = 1 token，实测约 0.6） | 保守方向；记录在案，不在本方案内改 |
| M5 只是读代码得出 | PR-5 先让作者真机确认再动 |

## 7. 台架：`scripts/local-model-probe.ts`

不是测试，不进 CI（有自己的 `scripts/local-model.vitest.config.ts`，`vitest.config.ts` 只收 `src/**`）。

```bash
LOCAL_LLM_URL=http://192.168.2.206:11234/v1 LOCAL_LLM_CTX=32000 LOCAL_LLM_UTIL=0.5 LIVE_SCENARIO=chat-bigdoc LIVE_LOG_DIR=/tmp/live pnpm exec vitest run --config scripts/local-model.vitest.config.ts
```

- 场景：`chat-edit` / `chat-longdoc` / `chat-bigdoc` / `chat-survey`（助手档）、`continue`（续写档）、`longform`（无工具）；`chat-session` 是六轮对话，每轮之前按 `sendChat` 的顺序调真的 `compactChatHistory`，量归纳频率与每次归纳的耗时。
- 旋钮：`LOCAL_LLM_EFFORT=off`（经应用自己的 `reasoningBody` 关思考）；`LIVE_TRIGGER_TOKENS`（把归纳线压到滑块够不着的位置，强制出现归纳）；`LIVE_FOLD_FORCE=1`（每轮都像「立即归纳」那样强制折，量单次摘要请求）。
- 托管模型做对照：再加 `LOCAL_LLM_KEY` 与 `LOCAL_LLM_MODEL`，`LOCAL_LLM_CTX` 照样压到 32000。**key 只走环境变量，不写进任何文件。**
- vitest 吞掉控制台输出，所以一切都落文件：`<场景>-<时间戳>.progress.log` 在运行中逐行增长（用来分辨「慢」和「挂了」），`.json` 在结束时写出完整 transcript、每个请求的首字 / 最长静默、每轮思考长度、重复调用数。
- LM Studio 串行处理请求：同时跑两个场景，两边的计时都不可信。
