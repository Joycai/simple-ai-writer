# 审批卡片的信息层 —— 给 Claude Design 的任务书

> 状态：`shipped` `unverified` · 2026-09-10 起草并发稿，同日设计稿回（`02h 审批卡片 Approval Cards.dc.html`，TURN 1）；落地清单 A–F 全部落地（A–E 为 #565–#571）。`unverified`：窄栏与撤回后编辑器重载没有在真机上看过。刻意没做的一样：`1c` 的「最后改于」（文件层没有 mtime，见文末）。
>
> 起因：作者反馈「审批卡片（尤其是编辑类的，如修改文件、修改知识库条目）看不到编辑的具体内容」。
>
> 前置切片（不等设计稿、无论稿子怎么答都要做的）：**切片 1** diff 核 `src/lib/diff/`（#558）；**切片 2** 把已在手上的数据接到提案上——`RewriteProposal.original`、`EditProposal.matches`（每处命中的行号 + 上下文行）、`DeleteProposal.excerpt` / `files`（#558，已并）；**切片 3** 知识库写入的 before/after 通道——`ChangeRecord` 挂在 `ToolStep` 上，十个正文类 L1 写入各自交回改前改后。**§4 第 3、4 题的数据侧已经就位**：稿子只需决定怎么显示。
>
> 本文 `---` 以下是**发给 Claude Design 的任务书原文**，可整段粘贴。落地后请照 `composer-chips-ui-brief.md` 的样子，在本文上半部分补「设计稿怎么答的 / 落地清单 / 与稿子的出入 / 真机没验到的」。

## 设计稿怎么答的（`02h`，TURN 1）

一句话：**卡上只开一扇窗**——改动处前后各两行原文做定位，删掉的字站在新字上面，其余折起来只报数。

编辑类四张卡（`edit` / `rewrite` / `delete` / `insert`）共用一种语汇：**改动窗**。统一 diff（上下叠），不是左右两栏——「AI 改善条目」模态那种两栏在 880px 里成立，在 280px 栏里、在排着三张卡且上面还在流式输出的对话流里都不成立，**两栏是阅读器的形状**。上下叠的窗在 280 和 1100 之间是同一套语汇，只是行号列宽变了。

三条贯穿全稿的判断：

1. **删除侧永远排第一。** rewrite 卡里少掉的段落是第一扇窗；delete 卡把文件开头三行铺在删色底上。视线先过要失去的，再过要得到的。
2. **卡的最大高度是一个常数。** 正文区封顶（抽屉 60vh / 窄栏 40vh），展开在卡内滚动，页脚常驻。40 处改动和 6 万字重写，卡一样高。
3. **字级高亮是有条件的点亮，不是默认。** 改动行 ≤ 3 且相似度 ≥ 0.5 才亮；低于 0.5 时逐字对照是噪音，定位行改写「整段替换」。

知识库那半推荐 **方向 D（`1i`）**：方案卡不长内容、写入照常落盘，但**每一步在方案卡原地长出回执**（已写入 · +9 −1 · 展开 · 撤回），只有「删条目」和「替换超过六成正文」两种破坏性步骤才升级成一张事前内容卡。稿子把四个方向摆在同一条线上判的：A（方案里就带正文）不成立——方案先于生成，要带正文就得先跑完 20 次生成，且落盘正文与卡上正文不保证一致，付出全部代价换到的仍是一句更长的承诺；B（每步一卡）是橡皮图章，第 5 张之后没人读；C（纯事后日志）不是审批，但它是 D 的底座。

自动批准之后（`1j`）：同一扇窗搬进执行日志的可展开行，**轮末一条汇总**——那是自动批准模式下作者唯一会看的一行，所以红色的数字（少了多少字、删了几段）必须留在汇总里。

`1z` 是能直接实现的规则表（几何 / 粒度与点亮 / 字号与颜色 / 各 kind 的头部度量 / 中英措辞 / 页脚不变项），实现一律照它。

**没动的十张卡**：`docx` / `xlsx` / `pptx` / `convert` / `transcribe` / `illustrate` / `append` / `create` / `move` / `copy`。稿子给了不冲突的理由：它们的正文是**规格表**（键值行），改动窗是**文本对照**（带行号的 mono），两种正文不会同卡出现；docx 那种「旧值 → 新值」的写法保留给规格类，不换成加删双色。

## 落地清单（一片一个 PR）

前三片在设计稿之前就落了（数据侧，见上面的「前置切片」）。剩下的按稿子分：

| 片 | 内容 | 状态 |
|---|---|---|
| **A** | 改动窗（`1z` A/B/C 的几何与点亮规则）+ `edit` 卡重做 + 边界态（`1l`：40 处折叠、改动为空、只改空白）+ `edit` 的窄栏降级 | 已落 |
| **B** | `rewrite` 卡：段级 diff、四类窗（删 / 合并 / 加 / 标点）与排序、「看改动 / 看成文」页签 | 已落 |
| **C** | `delete` 卡（单文件露开头三行 + 反向链接；文件夹露清单）+ `insert` 对齐写法 | 已落（`最后改于` 缺 mtime，见下） |
| **D1** | 知识库方向 D 之一：方案批准后留在执行日志里当账本，每步长回执（已写入 · +a −b · 展开看改动）、被门拒掉的写入也列出来 | 已落 |
| **D2** | 方向 D 之二：破坏性步骤（删条目 / 替换 ≥ 六成正文）写入前停下，方案里就地长出嵌套内容卡，按钮只有「跳过这步 / 批准」 | 已落（与稿子的出入见下） |
| **D3** | 方向 D 之三：撤回——单步撤回、撤回全部 | 已落（能说与不能说的见下） |
| **E** | 自动批准之后：执行日志的可展开行、轮末汇总、「在编辑器里打开对照」 | 已落（与稿子的出入见下） |
| **F** | 窄栏收尾：`rewrite` / 方案卡 / 页脚三行布局 | 已落（与稿子的出入见下） |

`1z` F 点名的三项新增数据依赖，两项已经有了：`edit` 的行号在切片 2 落地（`EditProposal.matches`），方案卡每步的备份路径与前后正文在切片 3 落地（`ChangeRecord`）。还缺的是 **`edit` 的 § 标题**（片 A 补）和 **delete 的反向链接查询**（片 C 补，`lib/fs/links.ts`）。

**片 D 拆成三片的原因。** 稿子的方向 D 是三件风险很不一样的事：账本是只读的；破坏性步骤的暂停是给 L1 写入加一个新的阻塞点；撤回是从备份恢复——而稿子没有回答**写入之后文件又被改过**时怎么办（作者手改了、或同一轮后面的步骤又写了一次；删掉的条目名又被新建占用了）。直接用备份覆盖会把之后的改动静默吞掉，那正是这一整套卡片要防的事。D2 的默认按现有规矩走（删除类永远不吃「本次都批准」，见 `autoApprove.ts`）。**D3 的冲突规则已定（2026-09-10）**：文件在那次写入之后变过，就拒绝撤回并说明谁在什么时候改过；只有文件还是那次写入的原样时才允许撤回。

**片 D2 与稿子的出入。**

- **嵌套卡出现在输入框上方的审批区，而不是长在账本里。** 运行被卡住的决定得出现在所有卡片出现的地方——通知与 surface 路由都指向那里；账本在执行日志里，而那一轮的日志可能已经滚到屏幕外。
- **账本不显示「等你批准 ↓」。** 卡着的那次工具调用还没有步号（门的命中是在调用结束时才抄进日志的），要显示得把方案 id 带进门里；账本在它结束后照常显示「已写入」或「已跳过」。
- **正文不到 200 字的条目不停**（`MAJOR_REWRITE_MIN_CHARS`）。稿子没有下限，但替换 100% 最常见的场景是把「待补充」补成正文，在那里停下来只会教作者不再读这张卡。
- **不能显示卡片的 surface 维持今天的行为，不拦截。** 目前带方案门的 surface——对话、任务面板、以及从两者派出的工具包（继承父级的卡片通道）——都能显示卡片，所以实际上处处会停。
- **删除条目里的单个特征文件（`delete_lore_file`）不停**，严格按稿子的两类。

**片 D3 与约定规则的出入。** 约定是「文件在写入之后变过就拒绝撤回，并说明谁在什么时候改过」。拒绝与只撤回原样文件这两半都做到了（`lib/agent/undo.ts`，判定靠 `ChangeRecord.afterHash`——正文被记录上限丢掉之后指纹还在）。「谁」和「什么时候」只做到一部分：

- **能点名的只有本轮稍后的写入**——「本轮稍后的「修改知识库文件」又改过这个文件，先撤回那一步」。作者手改的只能说「可能是你手动改的」。
- **说不出什么时候。** 文件系统层没有修改时间（片 C 已记），要补得动 Rust 侧的 `FileNode`。
- **撤回全部按从新到旧的顺序做**，所以同一个文件被写两次时，先撤回后一次，前一次就不再被它挡住。
- **撤回本身也先备份**它要覆盖的那一份；撤回之后不提供「重做」。
- **只有对话里的账本能撤回。** 任务面板的日志与知识库弹窗里的执行日志仍是只读账本；运行还没结束时也不给撤回按钮——下一次写入可能正好落在要恢复的文件上。
- **删条目现在也留一条（整个文件夹的）记录**，所以能被撤回；这之前的删条目没有记录，撤不回。这之前的正文写入若超过记录上限、又没有指纹，会以「没法确认文件还是写入时的样子」拒绝。

**片 E 与稿子的出入。**

- **带子在执行日志末尾，运行结束之后才出现。** 跑着的时候不显示：日志尾巴本来就是实时的账，一个还在变的总数不算总数。**每一轮写过东西都有**，不只是自动批准的那几轮——半小时前点过批准的卡，在对话记录里并不比没出现过的卡好找。
- **属于方案步骤的写入不重复列行**（账本里已经有了），但计入「本轮写入」的汇总。
- **进带子的是产生前后两份文本的写入**：`propose_edit`、`rewrite_lines`、`insert_lines`、`append_file`、`rewrite_document`、新建与删除文档，外加知识库写入。移动、复制、新建文件夹不在里面。
- **「在编辑器里打开」只在带子的展开行里**，审批卡上不给：批准之前不希望作者离开卡片。删除行与已撤回的行没有这个按钮。
- **章节超过记录上限（4000 字）时存的是窗口本身**（`ChangeRecord.diff`：最多 6 扇、每侧前两行、每行截到 300 字）。展开看得到改动，但窗内的「还有 N 行」点不开；超出 6 扇的只报数。
- **「撤回本轮全部」覆盖本轮所有写入**，规则与 D3 相同。撤回一个被删除的文档时，跟着它一起挪进备份的配图不会被放回。
- **顺手修掉的一个老问题。** 批准一个正打开着的文档时，改动以前靠 2 秒后的自动保存落盘，而批准之后读回文件的那些东西（给模型的行号回显、这次的改动记录与指纹）读到的是改之前的文字。现在批准后立即保存。撤回之前也先保存编辑器里没存的字（有未保存的字就按「改过了」拒绝），撤回之后重新载入打开的文档；撤掉的是新建出来的那个文档时，编辑器关掉它。

**片 F 与稿子的出入。**

- **「还有 N 处 · 在抽屉里看全部」没有做成把作者送去抽屉。** 审批卡和方案卡只长在 AI 抽屉里（对话、任务面板、扮演）；卡变窄是因为抽屉本身在小窗口里被压窄（宽 = min(1180, 94vw)），或者任务面板的结果列——作者已经在主场里，没有更宽的地方可去。窄的时候折叠行照旧是卡内「展开全部」。
- **重写窗在窄的时候每行截成一行**（稿子画的省略号），同时**每扇窗头多一个「展开」**，点开后整扇窗不截也不折。稿子没画被截掉的字怎么看——删掉的段落只露半句而没有地方看全，正是这套卡片要防的事。方案里嵌套的「替换六成正文」卡用同一套窗，一样处理。
- **头部第二行**：文件名后面接从第一行挪下来的规模——重写的 12 400 → 11 588 字、编辑的 18 → 30 字（或「40 处」）、方案步骤的「第 3 / 3 步」、文件夹的文档数。编辑卡的行号仍在正文的定位行里，没有重复进头部。
- **页脚三行**套在所有审批卡和方案卡上。「本文件都追加」「某程序都批准」这两个窄授权也降成同一行小字；配图的「批准并连批 N 张」带着张数下拉，保持控件样子，居中放在第三行。
- **方案卡在窄的时候**：步骤从三列变两列，动词 + 目标一行，说明另起一行。

片 C 少做了一样：`1c` 定位行里的「最后改于 8 月 3 日」。文件系统层**根本没有 mtime**——`FileNode` 只有 name/path/is_dir/children，取它要动 Rust 侧，而那棵树在每次打开项目时整棵读一遍，给每个条目加一次 metadata 调用是有代价的。行数与反向链接两句已经把那一行撑住了；要补 mtime 请单开一片。

## 起草时查到的事实（发稿前的底稿，不必粘贴）

- L2 手稿类提案共 14 种 kind，卡片实现在 `src/components/ai/ApprovalCard.tsx`（1042 行，一框 + 按 kind 分身的 body）。
- L1 知识库/记忆类写入**不出卡**：`writeTools.ts` 调用即落盘，事前由 `PlanCard` 一次性授权，事后只在执行日志里留一行（`logFormat.ts`：参数截断 400 字、结果截断 600 字的原始 JSON）。
- 手动路径的「AI 改善条目」模态早就有左右两栏 before/after（`LoreImproveModal.module.css` `.diffGrid` / `.diffPaneAfter`，设计稿 03a 屏 09/17）——同一件事在模态里看得见，在 agent 里看不见。
- 仓库里**没有**任何 diff 算法（`grep myers|diffLines|computeDiff` 空），字级/行级对照要新写或加依赖。
- diff 令牌已存在：`--color-diff-add-bg/text`、`--color-diff-del-bg/text`（`tokens.css`，纸色与夜色各一套），知识库侧另有 `--lore-add-bg`。

---

# 设计任务：在本项目里新建 `02h 审批卡片 Approval Cards.dc.html`

（AI 区当前最高是 `02g 助手输入区 Composer.dc.html`，所以取 `02h`；若已被占用，往后取第一个空位并在回稿里说明。）

## 0. 一句话

作者说：「审批卡片看不到编辑的具体内容。」这一稿要重做**整个审批卡片家族的信息层**：让作者在按下「批准」之前看得见**这一次到底动了哪些字**——改了什么、删掉了什么、新增了什么——而卡片仍然是**一个决定，不是一个阅读器**。

## 1. 这套卡片是什么

一个本地写作应用（桌面端，手稿美学，全局无圆角，纸色/夜色两套主题，中文优先）。AI 助手是一个带工具循环的 agent，它的写操作分两层：

- **L2 手稿类**（改文档、新建文件、删除、导出 PPTX/Word/Excel、格式转换、配图、音频转写）→ **阻塞式审批卡**。工具循环停在这张卡上，批准才继续；拒绝时输入框里的理由原样回传给模型。**这就是本稿的主角。**
- **L1 知识库 / 记忆类**（改条目正文、改元数据、建特征、归集、改名、删条目…）→ **不出卡**，模型一调用就落盘，安全阀只有一份自动备份。授权方式是事前的一张**方案卡**：模型先提一份 N 步的方案，作者批准，之后每一次写入必须能对上方案里的某一步，否则被拒。

卡片出现在三个宿主，宽度差别极大：

| 宿主 | 可用宽度 | 说明 |
|---|---|---|
| AI 抽屉里的对话助手 | 抽屉 `min(1180px, 94vw)`，卡片约 1100 | 主场，`02b 助手多会话 Assistant Sessions` |
| 右侧 AI 栏 AiPanel | **可拖 160–500px，默认 280** | 最窄的地方，`02a` |
| 扮演页 RoleplayChat | 同抽屉 | 借同一个组件 |

同一时刻可能**排着好几张卡**（并行工具调用）。卡片是对话流里的一个块，上面是流式输出的正文，下面是输入区。

## 2. 卡片现在的外框（要保住）

```
┌ 1px 赭石描边，inset 底色，无圆角 ─────────────────────┐
│ 标题(12px 无衬线)  文件名(10px mono)      度量(11px mono 右端) │
├───────────────────────── 发丝线 ──────────────────────┤
│ 模型给的一句理由（11px，可无）                          │
│ ← 按 kind 变化的正文，本稿改的就是这一块 →              │
├───────────────────────────────────────────────────────┤
│ [拒绝理由输入框………] [拒绝] [本次对话都批准] [批准]      │
└───────────────────────────────────────────────────────┘
```

头部的「度量」是这张卡的一个数字，各 kind 自己决定它是什么（改动前后字数、几处、几张表、估价…）。**这三段结构和 footer 不要重做**，除非你的方案证明必须。

## 3. 问题：每种卡现在给了什么，作者看不见什么

### 编辑类（本稿的痛点）

| kind | 卡上现在有什么 | 看不见什么 |
|---|---|---|
| **edit** 改一段 | 新文本全文（绿底 mono）在上；原文折在一个「原文」折叠里；`rewrite_lines` 来的多一行「第 120–164 行」；`find` 重复时多一行「第 2 处 / 共 5 处」或红字「全部 5 处」 | **没有任何逐字或逐行对照**。改了一个标点和重写了整段，在卡上长得一模一样——作者要自己把两块 mono 文本在脑子里对齐。也看不出这段落在文档的哪里（除非来自 `rewrite_lines`）。 |
| **rewrite** 整篇重写 | 渲染后的新正文（markdown，超过 600 字折叠），上面一行「少了 812 字」/「多了 300 字」，少了的时候标红 | **看不见少的是哪 812 字。** 一次「顺一下格式」悄悄吃掉一节，作者手里只有一个数字。这是这套卡今天最大的洞。 |
| **delete** 删文件/文件夹 | 路径一行 + 「可从备份恢复」；头部写「3000 字」或「12 个文件」 | **看不见要删掉的是什么**：没有开头几行，文件夹没有文件清单。 |
| **append** 追加 | 只显示追加的正文（原文按定义没动） | 够用。 |
| **insert** 插入行 | 每处一行：`L120` + 插入的文本 + 它落在哪一行之前（引用了那一行原文） | **够用，而且是已经做对的样板**——请把它当作定位语汇的参考。 |
| **create** 新建 | 渲染后的新正文；空文件/空文件夹有专门的空态句 | 够用。 |
| **move / copy** | 两端路径 + 箭头 | 够用。 |

### 已经做对的那一半（请先读，再动手）

`docx` / `xlsx` / `pptx` / `convert` / `transcribe` / `illustrate` 六张卡都遵循同一条纪律：**把「批准之前唯一看不见的那样东西」提到卡上**。

- **docx**：一条「格式从哪来」的带子（默认格式最安静，模仿文件来的带赭石竖条）+ 一张最终规格表，被改过的行高亮；再加一行内容侧预检「12 块 · 标题 5 · 表格 0」——一份作者以为写了三张表的报告在这里写着「表格 0」。
- **xlsx**：每张工作表一行，右边写「数字 87 · 日期 12 · 公式 3」，`数字 0` 标红——因为打开 Excel 之前唯一看不出来的就是数字有没有被当成数字。
- **pptx**：源→目标、幻灯片张数、以及从源码里查出来的、导出时会丢或走样的写法（按规则折叠成一行一条，附行号）。
- **transcribe**：四行规格（文件 / 估价 / 去处 / 写到），因为这一步**要花钱**，卡必须在付费之前出现。

**新的编辑类卡要达到同一档信息密度。** 这一档的判据是：卡上那几行，是不是「批准之前唯一看不见、批准之后就晚了」的东西。

### 知识库那一半（作者点名的第二个痛点）

方案卡（PlanCard）每一步只有三样东西：

```
新建   角色 / 凯尔     补一条新角色，出场在第 3 章
更新   角色 / 莉安 / 外貌.md   把「金发」改成「银发」，与第 12 章一致
删除   地点 / 旧码头   与「港口」重复
```

也就是：动词 + 条目名（+ 文件名）+ **模型自己写的一句话**。**卡上没有一个字是将要写进文件的内容**——作者批准的是一句承诺，而写入在批准之后立刻发生。

批准之后，真正写进去了什么只出现在执行日志的一行里：工具名 + 条目名，展开也只有**截断到 400 字的原始 JSON 参数**和 600 字的结果字符串。作者要知道「莉安的外貌到底被改成了什么」，今天只能去翻文件本身，或者去 `.ai-writer/backups/` 里找那份备份自己比。

对照组：**同一件事在手动路径里早就有评审 UI**——「AI 改善条目」模态（设计稿 `03a 屏 09/17`）是左右两栏 before/after 对照，880px 宽的模态。agent 侧没有。

## 4. 这一稿要回答的问题（按重要性）

1. **编辑类卡的正文该长什么样**，才能同时满足「一眼看清动了哪些字」和「卡片是一个决定，不是一个阅读器」？请具体到：
   - 改动怎么呈现：逐行 diff？行内逐字 diff？只在改动处开窗口 + 上下几行原文？未改动区域怎么折叠？
   - 规模的上限规矩：40 处改动、6 万字整页重写时这张卡长什么样；什么时候折叠，折叠后那句「还有 N 处没显示」怎么写。
   - **删除侧的可见性是硬要求**：今天「没了的字」在任何一张卡上都不出现，这一稿必须给它一个位置。
2. **位置感**：这段改动落在文档的哪里（行号 / 上下文行 / 第几处共几处 / 在文档的哪个小节）。`insert` 卡已经做对了，其它编辑类卡还没有。
3. **知识库**：批准一次知识库改动时，卡上要能看见**将要写进条目的正文**，以及**它替换掉的正文**。请给至少两个方向并说明取舍：
   - 方向 A：方案卡长出内容——但方案是在动手**之前**提的，那时正文还没生成。请判断这条路是否成立，若成立要付什么代价（模型得先写完再提方案 = 一次白跑的生成）。
   - 方向 B：写入之前多一张轻量的内容卡（把 L1 的一部分降级成 L2）。代价是延迟和卡片数量：一次「整理 20 条」会变成 20 张卡，而**读不动的卡就是橡皮图章**，比没有更糟。
   - 方向 C：写入之后一条「已写入 · 这是改动 · 撤回」的可展开条（备份就在那儿，撤回是现成的）。代价是它不再是审批，是事后追认。
   - 也可以给第四种。请把每个方向摆在这条线上：**不要为了看见内容，把 L1 的低延迟和「一张卡管一整轮整理」这两件对的事毁掉。**
4. **自动批准之后**：作者可以点「本次对话都批准 / 本次任务都批准」，此后卡片不再出现。**那之后「改了什么」必须仍然能在某处读到**——执行日志里的一条可展开行？一条汇总？请一并画。这是最容易被漏掉的一屏，但它恰恰是作者最需要的时候。
5. **窄栏**：160–500px 的右侧 AI 栏里这些卡怎么活。左右两栏对照在 280px 里不成立，请给窄栏形态（同一套语汇的降级，不是第二套设计）。

## 5. 视觉与语汇约束（必须遵守）

- **全局无圆角**。卡片是 1px 赭石描边 + inset 底色的一个整框，**没有**彩色左竖条那种卡片语汇。唯一允许的圆是状态点和转圈。
- **已有的 diff 令牌就用它们**，不要引入第三套颜色：`--color-diff-add-bg` / `--color-diff-add-text`（纸色下是青草绿）、`--color-diff-del-bg` / `--color-diff-del-text`（赭红），知识库侧另有 `--lore-add-bg`。夜色下这两组已各自调过，不要写死颜色。
- **字号语汇**：卡标题 12px 无衬线；文件名 10px mono；头部度量 11px mono（等宽数字）；模型理由 11px 无衬线；diff 面 12px/1.8 **mono**（「审阅中的改动」用等宽，「已完稿的正文」用正文字体——`create` / `rewrite` 的渲染正文走 markdown 排版，这条区分要保住）。
- **措辞**：中文，陈述句，动词打头。不写「确定要…吗？」，不用惊叹号，不用「⚠️」。词表：文档（不是章节）、分组、知识库、条目（不是设定/词条）。英文要有对应，两种语言都不许把按钮压到逐字折行。
- **本项目没有 diff 算法**，字级/行级对照要新写或加依赖。请给每个方向标注它对 diff 粒度的依赖（无 / 行级 / 字级），我们好估成本。
- 卡片可能同时排着好几张，且上方正文还在流式输出——**卡不能高到把对话挤出屏幕**，这是「决定不是阅读器」那条纪律的实际来源。

## 6. 要画的屏

1. `edit` 卡：小改动（改一个词）与大改动（整段替换）两态
2. `rewrite` 卡：含「少了 812 字」的那种情况——这是本稿的样板题
3. `delete` 卡：单文件与文件夹
4. `insert` 卡：现状复核；要改动请说明为什么
5. 知识库：方案卡 + 你给的内容可见方案，至少两个方向各一屏
6. 自动批准打开之后的「事后可读」形态
7. 280px 窄栏下的 `edit` 与 `rewrite`
8. 边界态：折叠 / 展开 / 超长 / 改动为空 / 只改了空白字符
9. 夜色下的 diff 面（加与删两种底色在深色上的成色）

## 7. 回稿希望包含

- 每个方向一段取舍，并**点名你推荐哪个**（我们按 `1c` 这样的编号采纳）。
- 一张 `1z` 规则页：颜色 / 字号 / 折叠阈值 / 上下文行数 / 措辞规则，写成能直接实现的表。
- 明确指出你**没有**改的那几张卡（`docx` / `xlsx` / `pptx` / `transcribe` 等），以及它们和新语汇之间有没有冲突。

## 8. 附：给稿子用的真实示例数据

- **edit**：文档「第三章 潮声.md」，第 120–164 行，`find` 出现 3 次、改第 2 处；原文「她把灯笼举高了些，金发在风里散开。」→ 新文「她把灯笼举高了些，银发在风里散开，像一截落在肩上的月光。」
- **rewrite**：文档「设定稿 · 港口.md」，12 400 字 → 11 588 字（少了 812 字），模型理由「统一标点并合并重复段落」。
- **delete**：文档「废稿 / 第五章旧版.md」，3 042 字，可从备份恢复。
- **知识库**：条目「莉安」/ `外貌.md`，把「金发及腰，常年一身洗旧的靛蓝长袍」改成「银发及腰，常年一身洗旧的靛蓝长袍；左眉上有一道旧疤」；同一份方案里还有「新建 · 角色 / 凯尔」「删除 · 地点 / 旧码头（与「港口」重复）」。
- **模型的一句理由**（`reason`）示例：「按第 12 章的描写统一发色。」
