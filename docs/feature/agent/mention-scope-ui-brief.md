# `@` 引用选择器 · 作用域档位与排序 —— 任务书、设计稿回执与实装记录

> 状态：`shipped` · 2026-09-26 起草，同日设计稿回（`02i @引用作用域 Mention Scope.dc.html`），同日落地。

## 起因

作者原话：「工程目录里文件和条目太多，用 @ 引用时找不到我想要的条目。通过名称准确搜索可以解决，但很不方便。希望在 @ 时能限定是知识库条目还是文件，缩小范围。」

调研（2026-09-26）发现「找不到」其实是四件事叠在一起，`components/common/MentionPicker.tsx` 旧的 `filterMentions`：

| 现象 | 原因 |
|---|---|
| 不打字时只见条目 | 候选 = 条目 + 文件，条目在前；空查询 `slice(0, 10)` |
| 打了字还是一堆 | 只做 `includes`，无排序，仍取前 10 |
| 记得别名 / 分组也没用 | 只匹配名字；条目别名、文档所在分组都不参与（⌘K 两者都参与） |
| 想只看文件也做不到 | `AgentChat` / `RoleplayChat` 各有一份 `PickKind` + `matchesKind`，但自 02g 合并「+ 引用」以来 `openMentionFor` 只以 `null` 调用——过滤路径是死的 |

02g 的说明里写过「@ 选择器里本来就分组，没有信息丢失」；实现里从来只是右端一枚徽标。这一稿是把那句话兑现。

## 决定（设计稿 02i · 1z 决策注记）

1. **作用域是一行 chip，不是前缀。** 全部 / 条目 / 文档，候选里有图才多一档「图片」。语法照 ⌘K 的档位组（`design-system.md` → 全局搜索），同一条既有规矩「作用域是 chip，不是前缀」：⌘K 画过 `/` `#` 前缀，落地时删了——作用域藏在首字符里，输入一长就滚出视野，而且 `@` 自己已经是个前缀。
2. **不恢复三个「+」。** 02g 屏 1c 否掉它们的理由是「过滤该住在选择器里」，现在过滤真的住进去了，「+ 引用」不动。
3. **Tab 改义。** 选择器里 Tab 从前等于 Enter（选中当前项）。改为 Tab / Shift+Tab 切档、只有 Enter 选中，与 ⌘K 一致。这是键位上唯一的改义（行为上还多了几处：空档吞 Enter、空列表上 ↑↓ / Tab 被接管、运行中 Esc 先关选择器再谈停运行、弹窗与扮演里 Shift+Enter 不再选中——都在决定 6 与实装记录里）。↑↓ / Esc 不动；IME 组字期间 Tab / Enter / ↑↓ 不接管，Esc 照旧关选择器。
4. **每次打开回到「全部」，不跨次记忆。** ⌘K 记上次的档，因为它一次只开一回、chip 常在视野里；`@` 一句话里要开几十次，粘住的窄档是静默陷阱——作者要附图时会以为图不见了。同一次里改 query 不重置；「同一次」以 `@` 的位置为准——提名开着时另起一个 `@`（`@潮@`，或点到正文里另一个 `@` 后面打字）也是新开（第三轮 review：第一版只看开没开，新 `@` 会把旧档位带过去）。
5. **空查询按类交错，不按类分段。** 只有十行，分三段每段三行等于没分；交错让作者一眼看到两类都在，想只看一类就 Tab。打了字之后按分数排，交错退场。
6. **空档留下来。** 0 命中从前 = 选择器消失、键盘分支随之放手，作者不知道自己在哪一档、Tab 也失效。现在 chip 行常在，下面一行 mono 事实报别处的数（「条目里没有「夜航」· 文档里有 1 篇 · Tab 切过去」）；全为 0 写「没有匹配」。Enter 在空档吞掉——既不选中也不发送——**但只在别的档有命中时**：哪个档都没有命中，这个 `@` 多半只是个 `@`（「发给@某人」），Enter 照常发送，和选择器学会留下来之前一样。
7. **录音 / 视频归「文档」档。** 档位说的是「文件 vs 条目」；图片单列只因为它在列表里长得不一样、且能否出现取决于模型看不看得见图。徽标照旧写 音频 / 视频。
8. **排序复用 ⌘K。** `matchText`：子串 > 词首 > 子序列；权重 名字 ×1 / 别名 ×0.9（`searchLore`）/ 分组路径 ×0.6 / 整条相对路径 ×0.5（后两档都是 `searchFiles` 的）。库层照 ⌘K 逐词、每个词可落在不同字段（测试保留，为的是和 ⌘K 一致），**但宿主永远只送一个词**：空格结束提名（`findMention`），`@潮汐门篇 归途` 敲下空格那一刻选择器就关了——第二轮 review 才发现文档把库层的多词例子当成了可达功能。作者能打出来的「同时用分组和名字定位」是一个词跨过 `/`：`@潮汐门篇/第五` 与 `@潮汐门篇/归途` 都走**拆分档**——带 `/` 的词按最后一个 `/` 拆开，左半严格对分组、右半像任何查询一样对名字（含子序列），两半均分后 ×0.5；记得分组和标题里随便一个词就够，不必是标题开头（第三轮 review 指出只认路径子串时后一句搜不到，与 ⌘K 不一致；第四轮算出前一句也是拆分档胜出——名字里没有 `/`，词中的 `/` 必落在分组与名字的分界，右半对名字开头总是满分）。整条相对路径那一档只剩 `/` 在词首或词尾的词（`/第五`、`潮汐门篇/`），拆分读不了它们。两条路都把区间拆回名字与副行两处高亮。分组路径与整条路径**只认子串和词首、不认子序列**：目录上的子序列是噪音，而这里的命中有牙——Enter 会把作者的字替换成 `@[名字]` 并附上文件，`问下@小李` 按 Enter 不该落到 `正文/小镇/李家.md`；名字与别名保留子序列（`@chth` 找 `Chapter Three.md`，同 `searchLore`）。每个词取最优字段，不照 `searchLore` 名字优先短路——短路会让名字上的弱子序列压过别名上的整词。靠别名命中的条目，副行显示那个别名并高亮——否则名字上没有任何高亮，作者会当它是错的答案。
9. **没动的**：`@[名字]` 落字、材料行芯片、附件构造、候选集合（哪些文件 / 图进候选仍由宿主按模型能力决定）、扮演的「已常驻」、选择器的位置（对话 / 扮演在输入框上方，知识库弹窗在下方）。零新存储、零新偏好。

### 被否的方案

| 方案 | 为什么不 |
|---|---|
| 前缀 `@/` `@#` | ⌘K 走过一遍被删；两套作用域语法比一套差 |
| 恢复三个 `+` 按钮 | 02g 已否：三个入口换来的只是过滤，而过滤该在选择器里 |
| 只改排序不加档 | 排序解决「打了字找不到」，解决不了「我就想浏览一下有哪些文档」 |
| 分组标题 | 十行分三段等于没分；chip 把十行整个交给一类 |
| 记忆上次的档 | 见决定 4 |
| chip 放在材料行 | 材料行是「这条消息带什么」，chip 是「现在在挑什么」，混进去正是 02g 消灭的混排 |

## 实装记录（2026-09-26）

分支 `feat/mention-scope`，三片：

- **纯逻辑** `lib/search/mentionSearch.ts`：`scopeOf` · `availableScopes`（chip 行的**唯一**来源，三个宿主都只调它）· `cycleScope` · `mentionSub`（文档的分组路径副行，根目录为空）· `searchMentions(items, query, scope, projectPath, limit)` → `{ items, hits }`（`hits` 按下标给名字 / 副行的高亮区间和命中的别名）· `countByScope`。结构化的 `MentionLike` 而不是组件的 `MentionItem`：`lib` 不 import `components`（`layering.test.ts`）。`mergeRanges` 从 `globalSearch` 导出供合并跨词区间。
- **选择器** `components/common/MentionPicker.tsx`：`useMentionState` 增 `scope` / `setScope` / `cycleScope`；状态收成一个对象，`sync` 是纯函数 `syncMention(prev, value, caret)` 在函数式 `setState` 里跑——从提交过的状态出发分辨「新开还是继续」：新开回「全部」**与第 0 行**（第一版只重置档位不重置高亮，Tab 到条目、↓↓、打「，」再退格，高亮会落在全部档另一项上），继续保留档位、改 query 回第 0 行；`close` 用 `useCallback` 稳住，点外关闭的监听不再随对话助手每次流式刷新重挂。空档那一行是独立的 `EmptyLine` 组件（测试不用绕 portal）；英文下 chip 是首字母大写的复数（Entries / Documents），句子里是小写复数——`appTerms` 的单数「entry」放进「3 in entry」读不通；从空档按 Tab 跳过同样空的档，「Tab 切过去」才是一下。选文件是先读完文件再落字，读的这段里提名可能已被关掉（打了「，」、Esc、点到外面），第二轮把位置搬进状态对象后关掉即清零，`@[名字]` 落到草稿开头还吃掉首字（第三轮 review 抓到）；第四轮起每个提名有序号（`MentionCore.id`，新开 +1，关掉保留），选中那一刻先 `claim()` 登记 `{id, start, query}`，读完再 `accept(value, item, claim, projectPath)` 落字——落回被 claim 的那个 `@`，读的期间另起的 `@夜` 不关、只按落字长度平移（第四轮的版本按快照落字且不平移，第七、八轮改成下面这样）；同一提名第二次 `accept`（慢文件上连按两下 Enter、双击一行）不再动正文，附件由宿主按 key 去重。落字本身是纯函数 `spliceMention`：`start` 处已不是 `@query` 就不动正文，附件照常；`@query` 后面的字**不看**——`findMention` 只读光标之前，句中插入的提名（`我想让@沈更生动`、光标在句中时点「+ 引用」）后面本来就有正文，第四轮加的「后一字必须是终止符」把这些一律拒掉，第五轮撤了。落下的 `@[名字]` 后面直接接着写是常态，`findMention` 把 `@[` 开头的一段、以及落在未闭合 `@[…` 里的 `@`（名字自己带 `@`：`封面@2x.png`）都当成已落的引用而不是 query——否则空档留在屏上的设计会让它以「没有匹配「[沈砚]的」」的样子浮起来吃掉 ↑↓ / Tab / 首个 Esc。未闭合的 `@[…` 到换行或中文标点为止（空格不算——名字里有空格），所以代价是作者自己打的 `@[` 及同一行到下一个「，」之前的 `@` 都不开、以 `[` 开头的名字要用里面的词搜（和以 `【` 开头的名字一样，`【` 本来就是终止符）——有意的取舍。等着读文件的 claim 按提名 id 记在一张表里（`trackClaims`，每次渲染跟着当前状态走）：提名开着时表项跟着作者打字更新（`@潮` → `@潮汐`），关掉后保留最后的位置，不管之后又开了几个别的提名都找得回来；同一个 `@` 上重开的提名（读的期间 Esc、再接着打，那个 `@` 换了新 id）也跟——它就是被 claim 的那个 `@`，与 `afterAccept` 的口径一致（第九轮）。一张表就是一份草稿：三个宿主都是一份草稿一个 `useMentionState`——对话助手按会话**重挂**（`AiDrawer` 的 `key={activeChatKey}`），扮演按角色重挂——所以表里不必分草稿。第九、十轮曾以为对话助手一个 hook 跨多个会话，先加「落不上再按快照位置试」的兜底、再按草稿分账（`MentionClaim.key` / `useMentionState(draftKey)`），第十一轮 review 查到重挂，两样都撤了；那两轮担心的「别的会话落字推动这边的 claim」在真实宿主里不会发生——第六、七轮各用一个「最近打开的提名」ref，一旦作者打长之后又另起 `@夜`，被 claim 的那个打长到哪儿就查不到了。落字（`acceptPick`，纯函数）按表里的**当前**位置：先前的落字已经把它挪过（两个慢文件同时在读，先落的把后一个的位置推后）；同一个提名打长了且按选择器自己的规则（`matchesMention`：名字 / 别名 / 分组 / 路径）仍找得到这一项就整段替换，找不到或替换不上就退回快照那段（打的是正文 `@潮的图`，正文保留）；**什么都没落上**（读的期间作者把 `@潮` 改成了 `@夜`）就不记账、不关任何提名——作者正看着的那个 `@` 仍是他的；只有真落了字才记「只落一次」、才把后面等着的 claim 和开着的提名按长出的长度平移（`afterAccept`：被 claim 的关掉、同一个 `@` 上重开的也关掉，后面的平移——宿主是程序化落字，不会再 `sync`）。整条协议有按宿主调用顺序驱动的序列用例。对话助手里读文件期间切了会话（或收起抽屉）：旧实例卸载了，它的 `handlePickMention` 仍会跑完，chip 与正文都落进**原会话**（`setRefs` / `setDraft` 绑定在它的 key 上）。正文必须落进该会话**此刻**的草稿——经 store 的 updater（`setDraft((now) => mention.accept(now, …))`，一次 store 操作，读写之间插不进别的改动，与扮演同一写法；第十一轮先写成 `getState()` 两步，第十二轮收成 updater），不能用实例的 `draftRef`——它冻结在切走那一刻，作者切回来（新实例）接着打的字会被整段覆盖；main 上就这样，第四轮起三轮都写成「切了会话只落 chip」，其实那道 `activeKeyRef` 门在重挂的实例里永远不成立（第十一轮 review）。旧实例的表在卸载后不再跟着打字：`@` 前面插了字就只落 chip，后面接着写则照常落上、后面的字保留，同一个 `@` 上打长的会留个尾巴——已知取舍。空 `@` 上在新实例里又挑了一次、新实例先落了字，旧实例读完不会叠成 `@[A][B]`：`spliceMention` 见到 `start` 处已是 `@[` 就不动，`spent` 按实例记、跨实例看不见（第十二轮）。但 `@[` 也可能是作者把 `@` 打在 `[草稿]第一章` 前面（`+ 引用` 时光标在 `[` 前）——`findMention` 只读光标之前，这个空 query 的提名是活的（第十三轮 review：第十二轮那句「活提名后面不可能紧跟 `[`」不成立，守卫误伤了它）；所以 claim 时记下 `glued`（那一刻 `@query` 后面是不是已经有 `[`），`glued` 的不走守卫、照常落成 `@[沈砚.png][草稿]第一章`；同一个 `@` 上真落过一次之后，还在等的 `glued` claim 改回过守卫——紧挨的 `[` 已是刚落下的引用（第十四轮：否则 Esc、重开、再选一个条目，慢的那个读完叠成 `@[A][B][草稿]`）。跨实例时卸载的实例看不见新实例在同一个 `@` 上的落字，`glued` 的 claim 仍会叠在前面——要 `@` 紧挨 `[`、读的期间切走又切回、同一个 `@` 上再选一次三件事同时成立，已知取舍。旧实例落字是别的实例写 store，新实例开着的选择器不会自己知道：`AgentChat` 记自己最后写进去的**值**（`ownDraft`——第十二轮用布尔标记，一次没让草稿变化的写会把它卡在 true、吞掉下一次别人的写），草稿变了又不是自己写的就按**改了哪一段**平移（`editRange`：新旧草稿的最长公共前后缀夹出被替换的那一段，落一个引用就是一段，界是精确的；`shiftCore` / `shiftClaims`：在那段之后的开着的提名与等着的 claim 加上长度差——提名保留 id / 档位 / 高亮，被那段盖住的提名关掉——紧贴 `@` 后落下的也算，空 query 的提名就是这样，否则它留着、下一次选中把刚落的 `[` 当成正文记成 `glued`、叠成 `@[B][A]`（第十五轮）；`@` 本来就紧跟 `[草稿]` 时落下的 `[潮汐.png]` 与旧文共用那个 `[`，贪心的公共前缀把段起点多推一格，所以比的是纯插入可能的最左位置 `lo`（第十六轮）；被盖住的 claim 留在原处等落字时核对失败，并且丢掉 `glued`——它后面的 `[` 已是刚落的引用）。第十二轮直接 `sync`、第十三轮按光标 `relocate`，都只对「开着、且在草稿末尾」的提名成立：已关掉的 claim 不动、句中的以错 query 重开、同 query 出现两次会挪到没选过的那个 `@` 上（第十四轮 review）。chip 行固定在顶上、行列表在它下面单独滚动（第一版把 chip 行 sticky 在滚动区里，`scrollIntoView({block:"nearest"})` 不认 sticky 头，↑↓ 绕回第 0 行时它藏在 chip 下面）；空档渲染 chip 行 + 一行事实；名字与副行按区间高亮，用的是和 ⌘K 同一个 `common/Highlighted`（`--color-mention-bg` 底 + `--color-sienna` 字；第一版各抄一份，两份的色 token 已经不一样了，review 时合并）。chip 走 `onMouseDown` + `preventDefault`，和行项一样不让 textarea 失焦。同文件还出两样给宿主共用：`useMentionSearch(candidates, mention, projectPath)` 跑作用域 / 打分 / 计数（选择器不在屏上时不算——候选每次条目写入、文件树刷新都在变，为没人看的列表重打分是纯浪费），`mentionKeyDown(e, mention, search, composing, onPick)` 是键盘协议的**唯一**一份，返回「这一键是不是选择器的」。
- **三个宿主** `AgentChat` / `RoleplayChat` / `lore/ai/AttachmentTextarea`：删私有 `PickKind` / `matchesKind`；各调一次 `useMentionSearch` 与 `mentionKeyDown`，只在「选中之后做什么」上不同。协议本身：Esc 在 IME 组字期间也关选择器（从前如此，否则对话助手运行中的 Esc 会去停运行），其余键组字期间不接管；Tab → `cycleScope`；Enter 在空档只在别处有命中时吞掉（`hasHits`）。门是 `search.open` = `mention.open && candidates.length > 0`：空档也开着（chip 行留着，作者才能 Tab 出去），但**一个候选都没有**（新项目、或只有图而模型看不见图）时没什么可分档——选择器不出来、键照旧放过，和它学会留在空档之前一样；第一版在这种项目里打一个 `@` 会弹出只有一枚「全部」chip 的空框，吃 Tab 和方向键直到终止符。组字判断三处都走 `useImeGuard().isComposing(e)`：扮演面板原来拿它自己那个裸 `composing` 状态当门，那个只为镜像层服务，Windows 上 `compositionend` 先于同一下 Enter 的 keydown 到，它已经翻回 false（`lib/ime.ts`），提交拼音的那一下 Enter 会选中一行或把话发出去。`AttachmentTextarea.module.css` 里抽出 `MentionPicker` 之前留下的死 `.picker*` 规则一并清掉。
- **文案** `ai.mention.scopeAll` / `scopeImage` / `tabHint` / `switchOver` / `emptyIn` / `emptyScope` / `emptyAll` / `countLore` / `countText` / `countImage`；「条目」「文档」两个词走 `appTerms`，不进 locale。

**验证**：`lib/search/__tests__/mentionSearch.test.ts` 钉住别名命中、路径命中且排在名字命中后、带 `/` 的词走拆分档且高亮拆回两行、`分组/标题中间一个词` 也能命中而左半不认子序列、`/` 在词首词尾的走整条路径档（各有区间断言）、分组与路径不认子序列（`小李` 不落 `小镇/李家.md`）、库层多词跨字段（⌘K 一致性）、取最优字段、空查询交错、作用域硬过滤、media 归文档、计数、limit、`cycleScope` 环绕与空档跳过空档。选择器在 vite 预览里用假候选独立渲染核过四态：全部档交错列表（含「已常驻」与已附带的暗行）、文档档 + 命中高亮（名字与副行）、条目档空档的事实行、纸面浅色（第一版还核过「跨字段查询 `潮汐门篇 归途`」，那是绕过 `findMention` 直接喂 query 的结果，宿主打不出来——见决定 8）。状态迁移与键盘协议抽成纯函数之后有了单测（`lib/agent/__tests__/chatMentions.test.ts` → `syncMention`：新开回全部与第 0 行、继续保留档位、改 query 回第 0 行、关掉再重开也回第 0 行、开着时另起一个 `@` 算新开；`spliceMention`：原位落字、句中提名后面的正文原样保留、`@query` 已不在原位则不动；`afterAccept`：关被 claim 的与同一 `@` 上重开的、后起的平移、更早的与已关的不动；`trackClaims`：开着跟、关了留、同一 `@` 上重开的也跟、没 claim 的不记；`claimOf`：登记与取回是同一处；`acceptPick`：只落一次、按当前位置、打长整段替换、退回快照 query、只在当前位置落（不再试快照位置）、没落上不记账不平移、落上了平移后面的 claim；序列用例：两个慢文件同时读、打长后另起提名、读的期间改成别的名字、Esc 后同名打长、喂给 `accept` 的是此刻草稿就不丢字且不叠在已落引用上、别的实例先落字后按改动段平移（开着的保留档位、关了的、句中的、同 query 两个的 claim 都落回自己那个 `@`）、`@` 打在 `[草稿]` 前照常落而同一 `@` 上落过之后不再叠、`editRange` 的界、按分组收窄；`findMention`：未闭合 `@[` 到中文标点为止、query 含 `]` 不开；`findMention`：落好的 `@[名字]` 后面接着打字不重开；`mentionKeyDown`：不在屏上不接管、Esc 组字期间也关、组字期间其余键放过、Tab / Shift+Tab 切档、空列表上 Tab 带计数跳过空档、Enter 选中高亮行、越界退回第 0 行、Shift+Enter 放过、空档 Enter 只在别处有命中时吞掉、没算计数时放行；`EmptyLine` 与 `Highlighted` 用 `renderToString` 核输出（三种情形、哨兵拆分、只点在册的档、英文复数）；`useMentionSearch` 同法：零候选不开、关着不算、开着给可用档与计数）。宿主里从 keydown 到这两个函数的那几行、`AgentChat` 读完文件后经 store updater 拿此刻草稿那一行与 `ownDraft` 的 `external`，以及真实输入法下的时序，仍只有代码路径与手动核——预览里对话助手要有模型、扮演与弹窗要有项目。

### 未做、可做

- 名字里带方括号的引用落下后读不回来（`mentionText.ts` 的 `MENTION_RE` 与 `lore/generator.ts` 的剥引用正则都只认不含 `]` 的名字）——main 上就如此，落字形状本分支没动；改的话三处读取方要一起改。
- 落字之后光标跳到草稿末尾而不是停在 `]` 后（三个宿主都是程序化 `setDraft`）——main 上就如此。
- `searchLore` 仍是名字优先短路（决定 8 的差异只改在 `mentionSearch`），⌘K 那边同样的例子会排低——不在本任务范围。
- 空查询时条目不再天然靠前，扮演面板里作者多半 `@` 条目：打一个字或一个 Tab 就到了，先不为扮演单独设默认档。
